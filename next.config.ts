import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "export",
  trailingSlash: false,
  basePath: "/shivantra.github.io",
  assetPrefix: "/shivantra.github.io/",
};

export default nextConfig;
